apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: {{service.namespace}}
spec:
  type: LoadBalancer
  ports:
  - name: web
    port: 9090
    protocol: TCP
    targetPort: web
  selector:
    prometheus: prometheus
